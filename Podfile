# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'XMPPProviderManager' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for XMPPProviderManager
  pod 'XMPPFramework', :git => 'http://gitrepo.frind.it/frind/XMPPFramework', :branch => 'master'

end

target 'XMPPProviderManagerTests' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for XMPPProviderManager
  pod 'XMPPFramework', :git => 'http://gitrepo.frind.it/frind/XMPPFramework', :branch => 'master'

end
